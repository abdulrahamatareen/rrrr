<%= form_for(@fine) do |f| %>
  <% if @fine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fine.errors.count, "error") %> prohibited this fine from being saved:</h2>

      <ul>
      <% @fine.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fine_type %><br>
    <%= f.text_field :fine_type %>
  </div>
  <div class="field">
    <%= f.label :fine_amunt %><br>
    <%= f.text_field :fine_amunt %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
